<UserControl x:Class="Linkedlistsstory.Pages.Adventure"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Linkedlistsstory.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Conversation area -->
        <ListBox x:Name="MessagesList"
             ItemsSource="{Binding Messages}"
             Background="WhiteSmoke"
             BorderThickness="0"
             Margin="115,10,88,163"
             HorizontalContentAlignment="Stretch" SelectionChanged="MessagesList_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="10" Padding="10" Margin="5">
                        <Border.Style>
                            <Style TargetType="Border">
                                <!-- Default: system message -->
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="Background" Value="#EFEFEF"/>
                                <!-- If this is a player choice, align to the right and change colour -->
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsUser}" Value="True">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Background" Value="#D0E6FF"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Current options area -->
        <StackPanel Orientation="Vertical" Margin="10" VerticalAlignment="Bottom">
            <ItemsControl ItemsSource="{Binding CurrentNode.options}" Height="148" RenderTransformOrigin="1,0.5" HorizontalContentAlignment="Center">
                <ItemsControl.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </ItemsControl.RenderTransform>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding choice}"
                Command="{Binding DataContext.ChooseOptionCommand,
                          RelativeSource={RelativeSource AncestorType=UserControl}}"
                CommandParameter="{Binding}"
                Width="700"
                Height="50"
                Margin="0,5,0,0"
                Padding="10"
                FontSize="20"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>


    </Grid>
</UserControl>
